Implement pow(x, n), which calculates x raised to the power n (i.e., xn).

lass Solution(object):
    def myPow(self, x, n):
        """
        :type x: float
        :type n: int
        :rtype: float
        """
        return x**n
